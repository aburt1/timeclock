{
  "name": "recycling-time-clock",
  "private": true,
  "version": "1.0.0",
  "description": "Big-button time clock for a high school recycling work program",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:server dev:web",
    "dev:server": "npm run dev -w server",
    "dev:web": "npm run dev -w web",
    "build": "npm run build -w web && npm run build -w server",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "npm-run-all2": "^7.0.2"
  }
}
